# Hashcat 狀態輸出 (JSON) 解析說明

本文件詳細解析 Hashcat 在執行過程中輸出的 JSON 格式狀態資訊，幫助理解破解進度和系統效能指標。

---

## 一、JSON 狀態輸出概述

當使用 `--status --status-json --status-timer=60` 參數時，Hashcat 會定期輸出 JSON 格式的狀態資訊，包含：
- 會話資訊
- 破解進度
- 效能指標
- 硬體狀態
- 時間估算

### 完整 JSON 輸出範例

```json
{
  "session": "hashcat",
  "guess": {
    "guess_base": "?l?l?l?l?l?l?a?a?a?a",
    "guess_base_count": 1,
    "guess_base_offset": 0,
    "guess_base_percent": 100.00,
    "guess_mod_count": 1,
    "guess_mod_offset": 0,
    "guess_mod_percent": 100.00,
    "guess_mode": 3
  },
  "status": 3,
  "target": "hash.txt",
  "progress": [437277518729729, 648656151720000],
  "restore_point": 247726080,
  "recovered_hashes": [0, 1],
  "recovered_salts": [0, 1],
  "rejected": 0,
  "devices": [
    {
      "device_id": 1,
      "device_name": "NVIDIA GeForce RTX 5070",
      "device_type": "GPU",
      "speed": 18828536596,
      "temp": 67,
      "util": 100,
      "fanspeed": 59,
      "corespeed": 2805,
      "memoryspeed": 13801,
      "buslanes": 16,
      "power": -1
    }
  ],
  "time_start": 1764776374,
  "estimated_stop": 1764798440
}
```

---

## 二、欄位詳細解析

### 2.1 會話資訊 (Session Info)

| 欄位 | 範例值 | 說明 |
|------|--------|------|
| `session` | `"hashcat"` | 會話名稱，可用於恢復中斷的破解任務 |
| `guess.guess_base` | `"?l?l?l?l?l?l?a?a?a?a"` | 當前使用的密碼遮罩或字典 |
| `guess.guess_base_count` | `1` | 遮罩/字典的數量 |
| `guess.guess_base_offset` | `0` | 當前處理的遮罩/字典偏移量 |
| `guess.guess_base_percent` | `100.00` | 當前遮罩/字典的完成百分比 |
| `guess.guess_mod_count` | `1` | 修飾符數量（如規則、字符集） |
| `guess.guess_mod_offset` | `0` | 修飾符偏移量 |
| `guess.guess_mod_percent` | `100.00` | 修飾符完成百分比 |
| `guess.guess_mode` | `3` | 攻擊模式（3 = Mask Attack） |

**遮罩解讀**：`?l?l?l?l?l?l?a?a?a?a`
- `?l` × 6：前 6 位為小寫字母（a-z）
- `?a` × 4：後 4 位為所有可見字符（字母+數字+符號）
- 總長度：10 字元

---

### 2.2 破解進度 (Progress)

| 欄位 | 範例值 | 說明 |
|------|--------|------|
| `status` | `3` | 狀態碼（詳見下方狀態碼表） |
| `target` | `"hash.txt"` | 目標 hash 檔案 |
| `progress` | `[437277518729729, 648656151720000]` | `[已處理, 總搜索空間]` |
| `restore_point` | `247726080` | 恢復點（用於中斷後繼續） |
| `recovered_hashes` | `[0, 1]` | `[已破解數量, 總 hash 數量]` |
| `recovered_salts` | `[0, 1]` | `[已破解 salt 數量, 總 salt 數量]` |
| `rejected` | `0` | 被拒絕的候選密碼數量 |

**狀態碼對照表**：

| 狀態碼 | 狀態名稱 | 說明 |
|--------|---------|------|
| 0 | Initializing | 初始化中 |
| 1 | Autotuning | 自動調整參數 |
| 2 | Running | 執行中 |
| 3 | Paused | 暫停 |
| 4 | Exhausted | 搜索空間已耗盡 |
| 5 | Cracked | 已破解 |
| 6 | Aborted | 已中止 |
| 7 | Quit | 已退出 |

**進度計算**：
```
完成百分比 = (已處理 / 總搜索空間) × 100
            = (437,277,518,729,729 / 648,656,151,720,000) × 100
            ≈ 67.41%
```

---

### 2.3 效能指標 (Performance)

| 欄位 | 範例值 | 說明 |
|------|--------|------|
| `speed` | `18828536596` | 當前速度（Hashes/秒），約 **18.83 GH/s** |
| `temp` | `67` | GPU 溫度（攝氏度） |
| `util` | `100` | GPU 利用率（%） |
| `fanspeed` | `59` | 風扇轉速（%） |
| `corespeed` | `2805` | GPU 核心時鐘（MHz） |
| `memoryspeed` | `13801` | 顯示記憶體時鐘（MHz） |
| `buslanes` | `16` | PCIe 通道數量 |
| `power` | `-1` | 功耗（W），-1 表示無法讀取 |

**效能解讀**：
- **18.83 GH/s**（Giga Hashes per second）= 每秒嘗試 188 億次密碼
- **GPU 利用率 100%**：達到最大負載（-w 4 Nightmare 模式）
- **溫度 67°C**：在安全範圍內（通常 < 85°C）
- **核心頻率 2805 MHz**：接近 RTX 5070 的 Boost 頻率

---

### 2.4 硬體資訊 (Device Info)

| 欄位 | 範例值 | 說明 |
|------|--------|------|
| `device_id` | `1` | 設備 ID（對應 `-d 1` 參數） |
| `device_name` | `"NVIDIA GeForce RTX 5070"` | GPU 型號 |
| `device_type` | `"GPU"` | 設備類型 |

---

### 2.5 時間估算 (Time Info)

| 欄位 | 範例值 | 說明 |
|------|--------|------|
| `time_start` | `1764776374` | 開始時間（Unix timestamp） |
| `estimated_stop` | `1764798440` | 預計結束時間（Unix timestamp） |

**時間計算**：
```
開始時間：2025-12-04 XX:XX:XX
預計結束：2025-12-04 YY:YY:YY
預計耗時：(1764798440 - 1764776374) = 22,066 秒 ≈ 6.13 小時
```

**剩餘時間**：
```
已完成：67.41%
剩餘：32.59%
剩餘時間 ≈ 6.13 × (32.59 / 67.41) ≈ 3.0 小時
```

---

## 三、效能分析與優化建議

### 3.1 當前效能評估

**速度表現**：
- 實測速度：**18.83 GH/s**
- 理論速度（RTX 5070 + SHA-1 + -O）：約 15-20 GH/s
- **評估**：✅ 效能優秀，已達硬體極限

**GPU 狀態**：
- 利用率：**100%**（最佳）
- 溫度：**67°C**（良好，< 85°C 安全線）
- 風扇：**59%**（溫控正常）
- 核心頻率：**2805 MHz**（接近最大 Boost）

### 3.2 搜索空間分析

**遮罩**：`?l?l?l?l?l?l?a?a?a?a`（6 個小寫 + 4 個任意字符）

**搜索空間計算**：
```
?l (小寫字母) = 26 個字符
?a (所有可見字符) = 95 個字符

總搜索空間 = 26^6 × 95^4
           = 308,915,776 × 81,450,625
           = 648,656,151,720,000 (約 648.7 兆)
```

**完成時間預估**：
```
總搜索空間：648,656,151,720,000
當前速度：18,828,536,596 H/s
理論總時間 = 648,656,151,720,000 / 18,828,536,596
           ≈ 34,450 秒
           ≈ 9.57 小時
```

### 3.3 優化建議

#### ✅ 已啟用的優化
1. **-O 優化核心**：速度提升約 1.87 倍
2. **-w 4 Nightmare 模式**：GPU 利用率達 100%
3. **-d 1 指定 GPU**：避免使用低效能設備

#### 🔧 可考慮的優化
1. **縮小搜索空間**：
   - 如果已知密碼規律，使用更精確的遮罩
   - 例如：`?l?l?l?l?l?l?d?d?s?s`（後 4 位限定為 2 數字 + 2 符號）
   - 搜索空間縮小至：26^6 × 10^2 × 33^2 ≈ 337 億（減少 99.99%）

2. **混合攻擊（Mode 6）**：
   - 如果前 6 位可能是常見單詞，使用字典 + 遮罩
   - 範例：`hashcat -a 6 hash.txt dictionary.txt ?d?d?s?s`

3. **分段破解**：
   - 先嘗試弱密碼（純小寫、純數字）
   - 再嘗試複雜組合

---

## 四、實際應用範例

### 4.1 監控破解進度（Python）

```python
import json
import subprocess
import time

def parse_hashcat_status(json_str):
    """解析 Hashcat JSON 狀態"""
    data = json.loads(json_str)
    
    # 提取關鍵資訊
    progress = data['progress']
    speed = data['devices'][0]['speed']
    temp = data['devices'][0]['temp']
    
    # 計算進度百分比
    percent = (progress[0] / progress[1]) * 100
    
    # 估算剩餘時間
    remaining = (progress[1] - progress[0]) / speed
    remaining_hours = remaining / 3600
    
    print(f"進度: {percent:.2f}%")
    print(f"速度: {speed / 1e9:.2f} GH/s")
    print(f"溫度: {temp}°C")
    print(f"預計剩餘: {remaining_hours:.2f} 小時")
    
    return percent

# 使用範例
json_output = '''
{
  "session": "hashcat",
  "status": 3,
  "progress": [437277518729729, 648656151720000],
  "devices": [{
    "device_id": 1,
    "speed": 18828536596,
    "temp": 67
  }]
}
'''

parse_hashcat_status(json_output)
```

### 4.2 自動暫停與恢復

```python
import json
import subprocess

def monitor_and_control():
    """監控 GPU 溫度，超過閾值時自動暫停"""
    
    # 讀取 Hashcat JSON 輸出
    status = get_hashcat_status()
    temp = status['devices'][0]['temp']
    
    # 溫度控制
    if temp > 80:
        print("⚠️ GPU 溫度過高，暫停破解...")
        subprocess.run(["hashcat", "--session", "mysession", "--pause"])
    elif temp < 70:
        print("✅ 溫度正常，恢復破解...")
        subprocess.run(["hashcat", "--restore", "--session", "mysession"])
```

---

## 五、常見問題與解決方案

### Q1：為什麼 `recovered_hashes` 顯示 `[0, 1]`？
**A**：表示總共有 1 個 hash，目前尚未破解（已破解 0 個）。當破解成功時會變為 `[1, 1]`。

### Q2：`status: 3` 代表什麼？
**A**：狀態碼 3 表示「暫停」（Paused）。可能是：
- 手動暫停（按 `p` 鍵）
- 達到運行時間限制（`--runtime`）
- 程式自動暫停

### Q3：如何根據 JSON 判斷破解是否完成？
**A**：檢查以下條件：
- `status: 5`（Cracked）：已破解
- `recovered_hashes[0] == recovered_hashes[1]`：所有 hash 已破解
- `status: 4`（Exhausted）：搜索空間耗盡但未破解

### Q4：`power: -1` 是什麼意思？
**A**：表示無法讀取 GPU 功耗資訊，可能原因：
- 驅動版本不支援
- 需要管理員權限
- GPU 不支援功耗監控

### Q5：如何提高破解速度？
**A**：參考「三、效能分析與優化建議」章節，主要方法：
1. 啟用 `-O` 優化核心
2. 使用 `-w 4` Nightmare 模式
3. 縮小搜索空間（精確遮罩）
4. 升級硬體（更高階 GPU）

---

## 六、參考資料

- **Hashcat 官方文件**：https://hashcat.net/wiki/
- **狀態碼說明**：https://hashcat.net/wiki/doku.php?id=frequently_asked_questions#how_do_i_read_the_status_screen
- **JSON 輸出格式**：使用 `--status-json` 參數
- **效能測試**：使用 `hashcat -b -m 100` 測試 SHA-1 效能

---

**文件版本**：v1.0  
**建立日期**：2025-12-04  
**適用版本**：Hashcat 7.1.2  
**相關文件**：[Hashcat攻擊模式指令說明.md](./Hashcat攻擊模式指令說明.md)
